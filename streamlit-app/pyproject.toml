[project]
name = "streamlit-clients-app"
version = "0.1.0"
description = "Streamlit app pour gérer les clients via l'API"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.31.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]